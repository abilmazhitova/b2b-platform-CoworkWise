# ПОЯСНИТЕЛЬНАЯ ЗАПИСКА
## к дипломной работе на тему:
# «Разработка веб-платформы для аналитики локаций коворкингов на основе геопространственных данных»

---

**Выполнили:** Абилмажитова Анель Досымбек Камила Искакбаева Аклима  
**Научный руководитель:** _____________  
**Год:** 2026  

---

## СОДЕРЖАНИЕ

1. [Введение](#1-введение)
2. [Анализ предметной области](#2-анализ-предметной-области)
3. [Постановка задачи и требования к системе](#3-постановка-задачи-и-требования-к-системе)
4. [Выбор и обоснование технологий](#4-выбор-и-обоснование-технологий)
5. [Проектирование системы](#5-проектирование-системы)
6. [Описание реализации](#6-описание-реализации)
7. [Тестирование](#7-тестирование)
8. [Заключение](#8-заключение)
9. [Список использованных источников](#9-список-использованных-источников)

---

## 1. ВВЕДЕНИЕ

### 1.1 Актуальность темы

Рынок коворкингов в Казахстане демонстрирует устойчивый рост: по данным аналитических агентств, с 2019 по 2024 год количество коворкинг-пространств в Алматы увеличилось более чем втрое. Расширение удалённого и гибридного формата работы, рост числа фрилансеров и малого бизнеса формируют стабильный спрос на гибкие офисные пространства.

Вместе с тем выбор локации для открытия коворкинга остаётся одним из наиболее рискованных решений для инвестора: по оценкам, до 40% новых коворкингов закрываются в течение первых двух лет, и основной причиной служит неверная оценка потенциала местоположения. Традиционный подход — субъективный осмотр района, визуальная оценка пешеходного трафика и сравнение нескольких вариантов «на глаз» — не позволяет обработать многомерные данные и принять объективное решение.

Актуальность разработки аналитической платформы, объединяющей реальные данные о перемещениях людей, геопространственную инфраструктуру и рыночные ставки аренды в единый инструмент поддержки принятия решений, не вызывает сомнений.

### 1.2 Цель и задачи работы

**Цель:** разработать веб-платформу CoworkWise для B2B-аудитории (девелоперы, инвесторы, операторы коворкингов), которая на основе телекоммуникационных данных и геопространственного анализа автоматически ранжирует локации Алматы по потенциалу открытия коворкинга.

**Задачи:**
1. Исследовать предметную область и выявить ключевые факторы успешности коворкинга.
2. Разработать алгоритм многофакторного скоринга локаций.
3. Спроектировать и реализовать серверную часть (REST API) с хранением телеком-данных в реляционной СУБД.
4. Реализовать клиентскую часть с интерактивной картой, аналитическими инструментами и трёхязычным интерфейсом.
5. Интегрировать данные из открытых источников: OpenStreetMap (инфраструктура), Krisha.kz (коммерческая аренда).
6. Обеспечить развёртывание системы в Docker-контейнерах.

### 1.3 Практическая значимость

Платформа позволяет:
- сократить время первичного анализа локаций с нескольких недель до нескольких минут;
- сравнивать произвольный набор точек по единой системе метрик;
- получить обоснованный рейтинг на основе реальных данных о посещаемости, а не экспертных оценок.

---

## 2. АНАЛИЗ ПРЕДМЕТНОЙ ОБЛАСТИ

### 2.1 Факторы успешности коворкинга

На основе анализа литературы и реальных кейсов рынка Алматы были выделены пять ключевых факторов, определяющих потенциал локации:

| Фактор | Влияние на успех | Источник данных |
|--------|-----------------|-----------------|
| Пешеходный трафик | Основной — высокая посещаемость района означает органический поток клиентов | Данные телеком-оператора |
| Транспортная доступность | Высокое — близость метро и автобусных остановок расширяет охват аудитории | OpenStreetMap |
| Деловая инфраструктура | Среднее — кафе, университеты, бизнес-центры создают «экосистему» | OpenStreetMap |
| Конкуренция | Отрицательное — высокая насыщенность рынка снижает долю нового игрока | OpenStreetMap |
| Стоимость аренды | Отрицательное — напрямую влияет на ROI и срок окупаемости | Krisha.kz (парсинг) |

### 2.2 Обзор существующих решений

**CBRE / JLL Analytics** — коммерческие платформы для анализа коммерческой недвижимости; не адаптированы для казахстанского рынка, высокая стоимость доступа.

**Google Maps Business Intelligence** — общий инструмент, не предоставляет данных о реальном трафике по ячейкам и не имеет алгоритмов оценки потенциала.

**2GIS Pro** — покрывает Алматы, но API предоставляет лишь статические данные о POI, не включает телеком-сигнал.

**CoworkWise** занимает незаполненную нишу: казахстанская платформа с реальными телеком-данными, открытой геоинфраструктурой и локальными данными об аренде.

### 2.3 Источники данных

**Телекоммуникационные данные.** Оператор предоставляет агрегированные данные в формате `.xlsx`: количество уникальных пользователей в каждой географической ячейке (ZID) с разбивкой по дням недели и часам суток. Данные анонимны и не содержат персональных сведений.

**OpenStreetMap (Overpass API).** Открытая картографическая база данных, содержащая объекты инфраструктуры: остановки общественного транспорта, кафе, рестораны, университеты, торговые центры, спортивные объекты, существующие коворкинги.

**Krisha.kz.** Крупнейший казахстанский агрегатор объявлений о недвижимости. Данные о коммерческой аренде собраны методом парсинга: 455 объявлений с ценой аренды тг/м²/мес по 8 районам Алматы.

---

## 3. ПОСТАНОВКА ЗАДАЧИ И ТРЕБОВАНИЯ К СИСТЕМЕ

### 3.1 Функциональные требования

**FR-01 Аутентификация:** система должна обеспечивать регистрацию и авторизацию пользователей с хранением JWT-токена.

**FR-02 Загрузка данных:** администратор должен иметь возможность загружать `.xlsx`-файлы с телеком-данными через веб-интерфейс с указанием метки месяца.

**FR-03 Карта трафика:** система должна отображать интерактивную тепловую карту пешеходного трафика по ячейкам Алматы с фильтрацией по районам, времени суток и радиусу поиска.

**FR-04 Рекомендации:** система должна автоматически формировать список топ-15 локаций с оценкой потенциала (0–100), текстовыми преимуществами и метриками.

**FR-05 Сравнение:** пользователь должен иметь возможность выбрать несколько точек на карте и сравнить их по единой таблице с показателями трафика, конкуренции, аренды и итогового рейтинга.

**FR-06 Прогноз:** система должна отображать прогноз трафика по месяцам и рейтинг районов по темпу роста.

**FR-07 Панель администратора:** управление пользователями, назначение ролей, настройки системы.

**FR-08 Мультиязычность:** интерфейс должен поддерживать три языка — английский, русский, казахский.

### 3.2 Нефункциональные требования

| Требование | Значение |
|-----------|----------|
| Доступность | Веб-приложение, доступное через браузер без установки ПО |
| Производительность | Время ответа API — не более 5 секунд для аналитических запросов |
| Безопасность | Хэширование паролей (bcrypt), JWT, разграничение ролей admin/user |
| Масштабируемость | Контейнеризация Docker, независимое масштабирование сервисов |
| Переносимость | Запуск на любой ОС с поддержкой Docker |

### 3.3 Ограничения

- Данные об аренде представляют собой медианные «запрашиваемые» цены (455 объявлений Krisha.kz), а не фактические сделки.
- Телеком-данные предоставлены за 2 месяца (март–апрель 2023); прогностическая модель Linear Regression при расширении датасета будет автоматически переобучена без изменения кода.
- Система охватывает 8 административных районов Алматы.

---

## 4. ВЫБОР И ОБОСНОВАНИЕ ТЕХНОЛОГИЙ

### 4.1 Серверная часть

**Python 3.12 + FastAPI** выбраны как серверная платформа по следующим причинам:
- FastAPI обеспечивает автоматическую генерацию OpenAPI-документации и нативную поддержку асинхронных операций через `asyncio`;
- богатая экосистема Python для геопространственного анализа (GeoPandas, Shapely, scikit-learn) критически важна для ML-сервиса;
- высокая производительность (сопоставима с Node.js) при асинхронных I/O-операциях.

**SQLAlchemy 2.0 (async) + asyncpg** — ORM с поддержкой асинхронных запросов к PostgreSQL, что позволяет обрабатывать большое количество одновременных запросов без блокировки.

**PostgreSQL 16** — реляционная СУБД, обеспечивающая надёжное хранение телеком-данных (потенциально миллионы строк в `telecom_stats`), транзакционность и возможность будущего расширения с PostGIS.

**Alembic** — система управления миграциями схемы базы данных, обеспечивающая версионирование и воспроизводимое развёртывание.

### 4.2 Клиентская часть

**Next.js 15 (App Router) + TypeScript** — фреймворк для React с серверным рендерингом, файловой маршрутизацией и встроенной оптимизацией. TypeScript обеспечивает типобезопасность и снижает количество ошибок при разработке.

**Tailwind CSS v4 + shadcn/ui** — утилитарная CSS-система и набор доступных UI-компонентов на базе Radix UI; обеспечивают единый дизайн без необходимости писать кастомный CSS.

**Leaflet** — библиотека для интерактивных карт с поддержкой тепловых слоёв (leaflet.heat), GeoJSON-полигонов и кастомных маркеров. Выбрана как лёгкая альтернатива Google Maps (бесплатная, без ограничений по ключу API).

**Recharts** — декларативная библиотека графиков для React; использована для гистограмм трафика и линейных графиков прогноза.

**Zustand** — минималистичная библиотека управления глобальным состоянием; хранит фильтры карты, сохранённые точки и язык интерфейса с персистентностью в localStorage.

### 4.3 ML-аналитический модуль

ML-слой платформы реализован как **отдельный микросервис** (`b2b-ml`) на Python + FastAPI, содержащий три обученных sklearn-модели. Сервис обучается автоматически при загрузке новых телеком-данных и предоставляет REST API для бэкенда.

**K-Means (`sklearn.cluster.KMeans`, k=4)** — кластеризация телеком-ячеек по признакам плотности трафика, инфраструктуры, конкуренции и аренды. Кластеры автоматически упорядочиваются по убыванию плотности: High Potential (80–100), Good Potential (60–79), Moderate Potential (40–59), Low Potential (20–39). Скор внутри кластера дифференцируется по относительной плотности.

**Linear Regression (`sklearn.linear_model.LinearRegression`)** — прогноз суммарного трафика следующего месяца по временному ряду. Признак — порядковый номер месяца, нормализованный через `StandardScaler`. Выход: предсказанное значение + доверительный интервал 95% (±1.96σ).

**DBSCAN (`sklearn.cluster.DBSCAN`)** — выявление географических хотспотов: кластеров ячеек с высокой пространственной концентрацией активности. Работает на нормализованных координатах (lat, lon).

**GeoPandas + Shapely** — геопространственные библиотеки в бэкенде для извлечения признаков (feature extraction):
- привязка телеком-ячеек к районам через пространственное объединение (`sjoin`) с полигонами `districts.geojson`;
- подсчёт объектов инфраструктуры в радиусе 700 метров — признаки `infra_score`, `accessibility`, `competition`.

**scikit-learn (MinMaxScaler)** — нормализация признаков в диапазон [0, 1] для резервного geo-скоринга.

### 4.4 Инфраструктура

**Docker + Docker Compose** — контейнеризация четырёх независимых сервисов: frontend (Node.js 20), backend (Python 3.12), ml (Python 3.11 + scikit-learn), database (PostgreSQL 16 + PostGIS). Обеспечивает воспроизводимое развёртывание и изоляцию зависимостей.

**PostgreSQL + PostGIS** — расширение PostGIS добавляет тип `Geometry` и пространственные функции (`ST_Within`, `ST_GeomFromGeoJSON`, GiST-индекс). Используется для фильтрации телеком-ячеек по границам районов непосредственно в СУБД без загрузки данных в Python.

---

## 5. ПРОЕКТИРОВАНИЕ СИСТЕМЫ

### 5.1 Архитектура

Система построена по четырёхзвенной микросервисной архитектуре, развёрнутой в Docker Compose:

```
Браузер пользователя
       │  HTTP :3000
       ▼
┌─────────────────┐
│  Next.js 15     │  b2b-frontend  (порт 3000)
│  (App Router)   │
└────────┬────────┘
         │  REST API  HTTP :8000
         ▼
┌─────────────────┐          ┌──────────────────────┐
│  FastAPI        │  HTTP    │  FastAPI ML-сервис    │
│  b2b-backend    │ ───────► │  b2b-ml  (порт 8001) │
│  (порт 8000)    │  :8001   │  K-Means, LR, DBSCAN │
└────────┬────────┘          └──────────────────────┘
         │  SQL (asyncpg)
         ▼
┌─────────────────┐
│  PostgreSQL 16  │  b2b-postgres  (порт 5432)
│  + PostGIS      │
└─────────────────┘
```

Фронтенд взаимодействует с бэкендом исключительно через REST API. Бэкенд делегирует ML-вычисления отдельному микросервису. ML-сервис персистирует обученные модели в именованном Docker-томе (`ml_models`).

### 5.2 Схема базы данных

```
┌──────────────────────┐         ┌───────────────────────────────┐
│       users          │         │        telecom_grids           │
├──────────────────────┤         ├───────────────────────────────┤
│ id          PK INT   │         │ id           PK INT            │
│ email       UNIQUE   │         │ zid_number   BIGINT UNIQUE     │
│ hashed_pw   STR      │         │ lat_bot_left FLOAT             │
│ full_name   STR?     │         │ lon_bot_left FLOAT             │
│ is_active   BOOL     │         │ lat_bot_right FLOAT            │
│ is_admin    BOOL     │         │ lon_bot_right FLOAT            │
└──────────────────────┘         │ lat_top_right FLOAT            │
                                 │ lon_top_right FLOAT            │
                                 └──────────────┬────────────────┘
                                                │ 1:N
                                 ┌──────────────▼────────────────┐
                                 │        telecom_stats           │
                                 ├───────────────────────────────┤
                                 │ id          PK INT             │
                                 │ grid_id     FK → grids.id      │
                                 │ week_day    INT (0–6)          │
                                 │ time_hour   INT (0–23)         │
                                 │ user_count  INT                │
                                 │ month_label STR?               │
                                 └───────────────────────────────┘
```

Таблица `telecom_grids` хранит географические координаты четырёх угловых точек каждой ячейки. Таблица `telecom_stats` содержит статистику уникальных пользователей с разбивкой по часу и дню недели для каждой загруженной партии данных.

### 5.3 API-контракт (основные эндпоинты)

**Backend API (порт 8000):**

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/auth/register` | Регистрация пользователя |
| POST | `/auth/login` | Вход, возвращает JWT-токен |
| GET | `/auth/me` | Текущий пользователь по токену |
| POST | `/telecom/upload` | Загрузка `.xlsx` + запуск ML-обучения в фоне |
| GET | `/telecom/grids/with_activity` | Ячейки с активностью (опционально PostGIS-фильтр по району) |
| GET | `/analysis/recommendations` | Топ-15 локаций (K-Means кластер + geo-скоринг) |
| GET | `/analysis/compare` | Топ-10 ячеек (K-Means по всей выборке + geo-фильтр) |
| GET | `/analysis/forecast` | LR-прогноз следующего месяца + рейтинг районов |
| GET | `/analysis/describe_point` | Характеристики точки по координатам |
| GET | `/analysis/hotspots` | DBSCAN пространственные хотспоты |
| POST | `/analysis/cluster_zones` | K-Means для произвольных зон (сессия карты) |
| GET | `/geo/district/{slug}` | GeoJSON полигон района |

**ML-сервис API (порт 8001, внутренний):**

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/health` | Статус + флаг `trained` |
| POST | `/train` | Обучить K-Means + LinearRegression на зонах и месяцах |
| POST | `/predict/clusters` | K-Means предсказание кластеров |
| POST | `/predict/forecast` | LR-прогноз следующего месяца |
| POST | `/predict/hotspots` | DBSCAN хотспоты |

### 5.4 ML-пайплайн

Система реализует двухуровневый пайплайн: обученные sklearn-модели в ML-микросервисе + геопространственный анализ в бэкенде.

#### Уровень 1 — ML-микросервис (b2b-ml)

**Обучение (после загрузки данных):**

1. Бэкенд собирает все телеком-ячейки из БД (плотность, координаты) и ежемесячные суммарные показатели.
2. K-Means (k=4) обучается на признаках `[density, infra_score, competition, rent_m2]`, нормализованных через `StandardScaler`. Кластеры упорядочиваются по убыванию средней плотности центроида.
3. Linear Regression обучается на временном ряду `[номер_месяца → суммарный_трафик]`.
4. Обе модели сохраняются в `.pkl`-файлы и сохраняются между перезапусками контейнера.

**Предсказание (при каждом запросе):**

- `predict_clusters`: K-Means назначает каждой зоне кластер → ранг (0–3) → метку (High/Good/Moderate/Low Potential) → скор в диапазоне ранга (80–100 / 60–79 / 40–59 / 20–39), дифференцированный по плотности внутри кластера.
- `predict_forecast`: LR предсказывает трафик следующего месяца + 95% доверительный интервал + процент роста.
- `predict_hotspots`: DBSCAN выявляет географические кластеры высокой активности по нормализованным координатам.

#### Уровень 2 — Геопространственный анализ (бэкенд)

**Шаг 1 — Данные из БД.**  
Суммарное количество уникальных пользователей на ячейку из `telecom_stats`.

**Шаг 2 — Геопривязка к районам.**  
`sjoin` центров ячеек с полигонами `districts.geojson` (EPSG:3857).

**Шаг 3 — Инфраструктурные признаки.**  
В радиусе 700 м подсчитываются объекты из `infra_points.csv`: `infra_score`, `accessibility`, `competition`.

**Шаг 4 — Аренда.**  
Медиана тг/м²/мес по справочнику Krisha.kz для района ячейки.

**Шаг 5 — Нормализация и geo-скоринг (резервный).**  
```
potential_score =
    0.30 × трафик_норм
  + 0.23 × инфраструктура_норм
  + 0.17 × транспорт_норм
  − 0.15 × конкуренция_норм
  − 0.15 × аренда_норм
```
Используется как fallback при недоступности ML-сервиса и для формирования текстовых преимуществ.

**Шаг 6 — Объединение результатов.**  
Бэкенд кластеризует ВСЕ зоны через K-Means (полная выборка), затем geo-скоринг отбирает топ-N. Итоговый ответ содержит ML-кластер и скор от ML-сервиса для каждой рекомендованной зоны.

### 5.5 Пользовательский интерфейс (основные экраны)

**Карта (`/map`)** — тепловая карта трафика на базе Leaflet с панелью фильтров (район, радиус, время суток). Режим Explore: клик на точку → аналитика. Режим Compare: выбор 2–5 точек для сравнения.

**Анализ (`/analysis`)** — четыре вкладки: Сравнение, Рекомендации, Сохранённые места, Прогноз.

**Администратор (`/admin`)** — управление пользователями, загрузка телеком-данных, системные настройки.

---

## 6. ОПИСАНИЕ РЕАЛИЗАЦИИ

### 6.1 Структура проекта

```
b2b/
├── b2b-backend/
│   ├── app/
│   │   ├── api/          — FastAPI роуты (auth, users, telecom, analysis, geo)
│   │   ├── models/       — SQLAlchemy модели (User, TelecomGrid, TelecomStat)
│   │   ├── schemas/      — Pydantic-схемы запросов/ответов
│   │   ├── services/
│   │   │   ├── telecom_service.py     — запросы к БД, парсинг xlsx, PostGIS
│   │   │   ├── ml_analysis_service.py — GeoPandas geo-скоринг, PostGIS helper
│   │   │   └── ml_client.py           — httpx-клиент ML-микросервиса
│   │   ├── config.py         — DATABASE_URL, ML_SERVICE_URL, SECRET_KEY
│   │   ├── core/security.py  — JWT и bcrypt
│   │   └── main.py           — точка входа FastAPI
│   ├── data/
│   │   ├── districts.geojson  — полигоны 8 районов Алматы
│   │   ├── infra_points.csv   — OSM-инфраструктура (~6 000 объектов)
│   │   └── rent_index.json    — медиана аренды по районам
│   ├── migrations/            — Alembic-миграции (включая PostGIS geom-колонку)
│   └── requirements.txt
├── b2b-ml/                    — ML-микросервис
│   ├── app/
│   │   ├── main.py        — FastAPI: /health, /train, /predict/*
│   │   ├── ml_service.py  — KMeans, LinearRegression, DBSCAN + load/train/predict
│   │   └── schemas.py     — Pydantic: TrainRequest, ClusterRequest и др.
│   ├── Dockerfile
│   └── requirements.txt
├── b2b-frontend/
│   ├── src/
│   │   ├── app/           — Next.js App Router страницы
│   │   ├── components/    — React-компоненты
│   │   └── lib/
│   │       ├── api.ts           — axios-клиент с JWT и Content-Type логикой
│   │       ├── store.ts         — Zustand: фильтры карты, sessionComparePoints
│   │       ├── lang-store.ts    — i18n: язык + useT()
│   │       └── translations/    — en.ts, ru.ts, kk.ts
│   └── package.json
├── scraper/
│   └── krisha_scraper.py   — парсинг аренды с Krisha.kz
├── docker-compose.yml
└── .gitignore
```

### 6.2 Обработка телеком-данных

При загрузке `.xlsx`-файла через `POST /telecom/upload` сервис `telecom_service.py` выполняет следующие операции:
1. Читает файл через `pandas` / `openpyxl`.
2. Для каждой строки проверяет уникальность `ZID_NUMBER` — создаёт запись в `telecom_grids` при первом появлении. При создании ячейки строится WKT-полигон из четырёх угловых точек и сохраняется как `geom = WKTElement(POLYGON(...), srid=4326)` — PostGIS-геометрия.
3. Добавляет запись в `telecom_stats` с привязкой к ячейке, временными атрибутами и меткой месяца.
4. После завершения импорта `BackgroundTasks` запускает `_trigger_ml_training()`: собирает все зоны и месяцы из БД, вызывает `POST /train` на ML-сервисе. ML-сервис обучает K-Means и LinearRegression на актуальных данных.

### 6.3 Сбор данных об аренде (парсер Krisha.kz)

Скрипт `scraper/krisha_scraper.py` собирает объявления о коммерческой аренде в Алматы:
- Навигация по страницам листинга через URL-параметр `page`.
- Извлечение встроенного JSON из объекта `window.data` на странице.
- Парсинг цены по регулярному выражению `([\d\xa0\s]+)\s*[〒₸]\s*за\s*м²` с учётом неразрывных пробелов (`\xa0`), используемых Krisha как разделитель тысяч.
- Определение района по строке адреса объявления.
- Фильтрация складской и промышленной недвижимости.
- Агрегация: медиана по каждому из 8 районов.

Результат (455 объявлений, май 2026) сохранён в `b2b-backend/data/rent_index.json`:

```json
{
  "Medeu":     { "median_rent_m2": 12000, "count": 147 },
  "Bostandyq": { "median_rent_m2": 12100, "count": 101 },
  "Almaly":    { "median_rent_m2": 9000,  "count": 90  },
  "Auezov":    { "median_rent_m2": 7500,  "count": 31  },
  "Zhetysu":   { "median_rent_m2": 6667,  "count": 27  },
  "Alatau":    { "median_rent_m2": 6000,  "count": 17  },
  "Turksib":   { "median_rent_m2": 5000,  "count": 23  },
  "Nauryzbay": { "median_rent_m2": 4615,  "count": 19  }
}
```

### 6.4 Интернационализация (i18n)

Три языка реализованы через клиентскую библиотеку Zustand без серверных зависимостей:
- Словари хранятся в файлах `src/lib/translations/en.ts`, `ru.ts`, `kk.ts`.
- Тип `Translations` выводится из английского файла — TypeScript выдаёт ошибку при отсутствии ключа в русском или казахском словаре.
- Хук `useT()` возвращает нужный словарь по текущему языку из `useLangStore`.
- Выбранный язык сохраняется в localStorage и восстанавливается при следующей сессии.

### 6.5 Аутентификация и авторизация

- Пароли хэшируются через `bcrypt` с автоматической генерацией соли.
- При успешном входе выдаётся JWT-токен (подписан секретным ключом, срок действия — 7 дней).
- Токен хранится в localStorage фронтенда и передаётся в заголовке `Authorization: Bearer <token>` каждого запроса.
- Middleware FastAPI проверяет токен и декодирует пользователя для каждого защищённого эндпоинта.
- Роль `is_admin` требуется для управления пользователями и загрузки данных.

### 6.6 Развёртывание

Система запускается одной командой `docker compose up --build`:

```yaml
services:
  postgres:  # postgis/postgis:16-3.4-alpine, порт 5432
  ml:        # Python 3.11 + scikit-learn, порт 8001, том ml_models
  backend:   # Python 3.12 + FastAPI, порт 8000, depends_on: postgres, ml
  frontend:  # Node.js 20-alpine + Next.js 15, порт 3000, depends_on: backend

volumes:
  ml_models: # именованный том для хранения pkl-файлов моделей
```

ML-сервис имеет healthcheck (`GET /health`): бэкенд ждёт его готовности перед стартом. При первом запуске Alembic автоматически применяет миграции (включая `CREATE EXTENSION postgis` и добавление колонки `geom`).

---

## 7. ТЕСТИРОВАНИЕ

### 7.1 Тестирование API

Функциональность REST API проверялась через Swagger UI (автоматически генерируется FastAPI по адресу `http://localhost:8000/docs`).

**Проверенные сценарии:**

| Тест | Ожидаемый результат | Фактический результат |
|------|--------------------|-----------------------|
| POST /auth/register с корректными данными | 201, возвращает данные пользователя | Пройден |
| POST /auth/login с верным паролем | 200, возвращает JWT-токен | Пройден |
| POST /auth/login с неверным паролем | 401 Unauthorized | Пройден |
| POST /telecom/upload (.xlsx файл) | 200, данные сохранены в БД | Пройден |
| GET /analysis/recommendations без данных | 200, пустой список | Пройден |
| GET /analysis/recommendations с данными | 200, список из ≤15 локаций с полями score, metrics, advantages | Пройден |
| GET /geo/district/medeu | 200, GeoJSON FeatureCollection | Пройден |
| PATCH /users/{id}/role без роли admin | 403 Forbidden | Пройден |

### 7.2 Тестирование ML-моделей и скоринг-алгоритма

**K-Means кластеризация:**
- После обучения на 230+ ячейках сформированы 4 кластера с разными центроидами плотности. Скоры в таблице сравнения варьируются в диапазоне 80–100, 60–79 и т.д. — не совпадают у всех зон.
- Зоны с наибольшей активностью (Бостандык, Алмалы) получают кластер "High Potential"; зоны с минимальной активностью — "Low Potential".

**Linear Regression прогноз:**
- При двух точках данных (03.2023, 04.2023) модель корректно экстраполирует тренд на 05.2023.
- Доверительный интервал (±1.96σ) рассчитывается на основе остатков модели.

**Проверка geo-скоринга:**
- При равных значениях трафика, инфраструктуры и транспорта — район с дорогой арендой (Medeu: 12 000 тг/м²) получает score на 12–15 единиц ниже, чем район с дешёвой арендой (Nauryzbay: 4 615 тг/м²).
- Ячейки с нулевой активностью получают `avg_density_scaled = 0` и попадают в нижнюю часть рейтинга.

### 7.3 Тестирование пользовательского интерфейса

**Карта:** загрузка тепловой карты проверялась на партии телеком-данных с 2 847 ячейками. Время загрузки — менее 3 секунд.

**Сравнение:** проверена корректность отображения колонки «Аренда тг/м²» для всех трёх источников данных (кандидаты модели, сессия карты, сохранённые точки) — значения совпадают с `rent_index.json`.

**i18n:** переключение языков проверено для всех трёх языков на всех страницах; отсутствие непереведённых строк подтверждено TypeScript-компилятором.

**Адаптивность:** интерфейс проверен на разрешениях 1920×1080, 1366×768 и 375×812 (мобильный).

### 7.4 Тестирование парсера

Скрипт `krisha_scraper.py` был запущен на реальных страницах Krisha.kz. По итогам собрано 455 объявлений. Результаты проверены вручную: медианные значения по районам соответствуют рыночным ожиданиям (центральные районы — Medeu, Bostandyq — дороже, периферийные — Turksib, Nauryzbay — дешевле).

---

## 8. ЗАКЛЮЧЕНИЕ

В рамках дипломной работы была спроектирована и реализована веб-платформа CoworkWise для аналитики локаций коворкингов в Алматы.

**Достигнутые результаты:**

1. Реализован отдельный ML-микросервис (`b2b-ml`) с тремя обученными моделями scikit-learn: K-Means (кластеризация зон по потенциалу), Linear Regression (прогноз трафика), DBSCAN (пространственные хотспоты). Модели автоматически переобучаются при загрузке новых данных.

2. Разработан и реализован геопространственный алгоритм многофакторного скоринга локаций, учитывающий пешеходный трафик (вес 0.30), деловую инфраструктуру (0.23), транспортную доступность (0.17), конкуренцию (−0.15) и стоимость аренды (−0.15). Алгоритм используется в связке с ML-моделями: geo-скоринг отбирает кандидатов, K-Means присваивает категорию потенциала.

3. Реализован серверный REST API на FastAPI с асинхронным доступом к PostgreSQL + PostGIS. PostGIS используется для пространственной фильтрации ячеек по районам (`ST_Within`) непосредственно в СУБД.

4. Создан интерактивный клиентский интерфейс на Next.js 15 с тепловой картой трафика, ML-кластерами в таблице сравнения, прогнозом с доверительным интервалом и поддержкой трёх языков (русский, английский, казахский).

5. Интегрированы данные из трёх источников: телеком-оператор (трафик по ячейкам), OpenStreetMap (инфраструктура), Krisha.kz (455 объявлений об аренде по 8 районам).

6. Вся система контейнеризирована в четырёх Docker-сервисах и разворачивается одной командой `docker compose up --build`.

**Направления дальнейшего развития:**

- Переход от K-Means к надзорной модели (логистическая регрессия, градиентный бустинг) — при накоплении 50+ реальных кейсов открытия коворкингов с данными об успехе/закрытии. Архитектура ML-микросервиса для этого уже готова.
- Учёт сезонности в Linear Regression при наличии данных за 2+ года.
- Мобильное приложение на React Native с повторным использованием существующего API.

---

## 9. СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ

1. FastAPI Documentation. Sebastián Ramírez. URL: https://fastapi.tiangolo.com (дата обращения: 2026).
2. Next.js 15 Documentation. Vercel Inc. URL: https://nextjs.org/docs (дата обращения: 2026).
3. SQLAlchemy 2.0 Documentation. Michael Bayer. URL: https://docs.sqlalchemy.org (дата обращения: 2026).
4. GeoPandas Documentation. URL: https://geopandas.org (дата обращения: 2026).
5. OpenStreetMap Wiki. Overpass API. URL: https://wiki.openstreetmap.org/wiki/Overpass_API (дата обращения: 2026).
6. Scikit-learn: Machine Learning in Python. Pedregosa et al. Journal of Machine Learning Research, 12, 2011.
7. Docker Documentation. Docker Inc. URL: https://docs.docker.com (дата обращения: 2026).
8. Recharts Documentation. URL: https://recharts.org (дата обращения: 2026).
9. Leaflet.js Documentation. URL: https://leafletjs.com (дата обращения: 2026).
10. Zustand: State Management for React. Daishi Kato. URL: https://github.com/pmndrs/zustand (дата обращения: 2026).
11. Исследование рынка коворкингов Казахстана 2024. Colliers Kazakhstan. Алматы, 2024.
12. Krisha.kz — Коммерческая аренда Алматы. URL: https://krisha.kz (дата обращения: май 2026).

---

*Объём пояснительной записки: 9 разделов. Все фрагменты кода, схемы и данные получены непосредственно из исходного кода платформы CoworkWise.*